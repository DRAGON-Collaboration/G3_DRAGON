C.
      INTEGER iscnt
      REAL photon_yield, resolution_scale
C.
      INTEGER n_flag
      REAL e_detect, edetect
C.
      REAL E_threshold
C.
      INTEGER nloss(6)
C.
      REAL tot_thrshld, pmt_thrshld
C.
      COMMON / uenergy / iscnt, photon_yield, resolution_scale,
     &                   n_flag, e_detect, edetect, nloss,
     &                   tot_thrshld, pmt_thrshld,
     &                   E_threshold
C.
